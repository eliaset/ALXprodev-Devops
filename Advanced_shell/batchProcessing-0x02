#!/bin/bash

# batchProcessing-0x02
# Task 2 + Task 4: Batch Pokémon Data Retrieval with Retry & Error Handling

POKEMONS=("bulbasaur" "ivysaur" "venusaur" "charmander" "charmeleon")
OUTPUT_DIR="pokemon_data"
mkdir -p "$OUTPUT_DIR"
ERROR_LOG="errors.txt"
> "$ERROR_LOG"  # clear previous errors

for P in "${POKEMONS[@]}"; do
    echo "Fetching data for $P..."
    FILE="$OUTPUT_DIR/$P.json"

    RETRIES=0
    SUCCESS=0

    # Retry up to 3 times (literal 3)
    while [ $RETRIES -lt 3 ]; do
        # Use curl with timeout and silent mode
        HTTP_CODE=$(curl -s -S --connect-timeout 5 -m 10 -o "$FILE" -w "%{http_code}" "https://pokeapi.co/api/v2/pokemon/$P")
        
        if [ "$HTTP_CODE" -eq 200 ]; then
            echo "Saved data to $FILE ✅"
            SUCCESS=1
            break
        else
            echo "Attempt $((RETRIES+1)) failed for $P (HTTP $HTTP_CODE)"
            RETRIES=$((RETRIES+1))
            sleep 1  # short delay to avoid rate-limiting
        fi
    done

    # Handle complete failure
    if [ $SUCCESS -eq 0 ]; then
        echo "Failed to fetch $P after 3 attempts" >> "$ERROR_LOG"
        # Create a dummy JSON so the next tasks don't fail
        echo '{"error":"data not fetched"}' > "$FILE"
    fi
done
